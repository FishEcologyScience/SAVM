# sav_model() with full post-hoc works

    Code
      res3_a <- sav_model(df_ok2, method_pa = "gam", limitation = "custom")
    Message
      i Using substrate, secchi, and custom for post-hoc treatment.

