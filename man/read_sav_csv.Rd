% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_input.R
\name{read_sav_csv}
\alias{read_sav_csv}
\title{Read a CSV file and convert to sf object}
\usage{
read_sav_csv(file_path, crs = 3857, ...)
}
\arguments{
\item{file_path}{{\code{character}}\cr{} Path to the CSV file.}

\item{crs}{{\code{object}}\cr{} Coordinate reference system (CRS)
of the input data (see \code{\link[sf:st_crs]{sf::st_crs()}}). Default is 3857.}

\item{...}{Further arguments passed on to \code{\link[utils:read.table]{utils::read.csv()}}.}
}
\value{
An sf object with required and optional columns.
}
\description{
Read a CSV file and convert to sf object
}
\examples{
# Example CSV file creation
temp_csv <- tempfile(fileext = ".csv")
write.csv(data.frame(
    longitude = c(-82.5, -83.0, -83.2),
    latitude = c(42.5, 42.8, 42.6),
    depth_m = c(5, 10, 7),
    mean_fetch_km = c(2.5, 3.0, 2.8),
    turbidity = c(1.2, 2.3, 1.8),
    substrate_limits = c(TRUE, FALSE, TRUE)
), temp_csv, row.names = FALSE)

# Read the CSV and convert to sf
read_sav_csv(temp_csv, crs = 4326)
}
